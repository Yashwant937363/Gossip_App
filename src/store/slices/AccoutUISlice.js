import { createSlice } from "@reduxjs/toolkit";

const acccoutUISlice = createSlice({
    name : 'accoutUI',
    initialState : {
        
    },
    reducers : {

    }
})

const {} = acccoutUISlice.actions
export default acccoutUISlice.reducer